export const appContainer = "bg-white rounded-xl overflow-hidden shadow-xl flex flex-col h-[calc(100vh-64px)]";

export const headerWrapper = "flex-shrink-0";

export const scrollableContent = "flex-1 overflow-y-auto px-4 py-2 space-y-2";

export const fabWrapper = "flex-shrink-0 flex justify-center pb-6 pt-3 bg-gradient-to-t from-white to-transparent";

export const fabButton = "w-12 h-12 bg-primary hover:bg-primary-dark text-white rounded-full shadow-lg flex items-center justify-center transition-all duration-200 hover:scale-105 active:scale-95";
